# Build output from `go build -o mkprs .`.
/mkprs
/mkprs.exe

# JetBrains stuff
/.idea/

# macOS Finder metadata
.DS_Store

# `go test -c` binaries and coverage profiles
*.test
coverage.out

# graphify: commit the graph, ignore the caches and machine-local state
/graphify-out/*
!/graphify-out/graph.json
!/graphify-out/GRAPH_REPORT.md
